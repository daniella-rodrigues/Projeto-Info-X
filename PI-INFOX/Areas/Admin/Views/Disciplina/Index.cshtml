@model IEnumerable<PI_INFOX.Models.Basicas.basDisciplina>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (Session["nomeUsuarioLogado"] != null)
{
    <text>
       <h3> Bem Vindo @Session["nomeUsuarioLogado"].ToString() </h3>
    </text>
}

<div>
    <ul>
        <li>@Html.ActionLink("Criar Novo", "Create")</li>
    </ul>
</div>
<div>
    <ul>
        <li>@Html.ActionLink("Home","Index","home")</li>
    </ul>
</div>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DisciplinaID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nomeDisciplina)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DisciplinaID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nomeDisciplina)
        </td>
         <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.DisciplinaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DisciplinaID  }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DisciplinaID  })
        </td>
    </tr>
}

</table>
