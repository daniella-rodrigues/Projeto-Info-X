@model IEnumerable<PI_INFOX.Models.Basicas.basForum>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@if (Session["nomeUsuarioLogado"] != null)
{
    <text>
       <h3> Bem Vindo ao Forum @Session["nomeUsuarioLogado"].ToString() </h3>
    </text>
}
<div>
    
        @Html.ActionLink("Criar Novo", "Create",null, new { @class = "btn btn-success" })

</div>


<table class="panel panel-primary">
   
        <tr class="panel-heading">
            <td>
                @Html.DisplayNameFor(model => model.ForumID)
            </td>
            <td>
                @Html.DisplayNameFor(model => model.titulo)
            </td>

            <td>
                @Html.DisplayNameFor(model => model.Disciplina.nomeDisciplina)
            </td>
        </tr>
   
   
        @foreach (var item in Model)
        {
            <tr class="panel-body">
                <td>
                    @Html.DisplayFor(modelItem => item.ForumID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.titulo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Disciplina.nomeDisciplina)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Edit", new {  id=item.ForumID } ,new { @class = "btn btn-info" }) 
                    @Html.ActionLink("Veja Mais aqui", "Details", new { id = item.ForumID }, new { @class = "btn btn-info" })
                    @Html.ActionLink("Delete", "Delete", new { id=item.ForumID }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }
   
</table>